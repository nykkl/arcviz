// use super::ResourceHandle;

pub trait ResourceProvider {
	// fn get_reference(&self, id: &str) -> Option<ResourceHandle>;
	// fn get_or_insert_reference(&mut self, id: &str) -> ResourceHandle;
	// fn export_subset(&self, ids: &[String]) -> Self;

	// fn add(&mut self, id: String, data: ResourceInstance);
	// fn write(&self, writer: &mut ResourceWriter) -> Result<(),()>;
}
